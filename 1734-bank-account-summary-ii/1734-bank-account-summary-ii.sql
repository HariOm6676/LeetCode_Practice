# Write your MySQL query statement below
select name , balance from (select Users.name,sum(amount) as balance from Users inner join Transactions on Users.account=Transactions.account  group by Users.account) as tbl1 where balance>10000